const controller = require("./addresses.controller");
const model = require("./addresses.model");
const routes = require("./addresses.routes");
const service = require("./addresses.service");



module.exports = {
    controller,
    model,
    routes,
    service
};
